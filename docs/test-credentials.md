# Ladder — Test Credentials

**WARNING: These accounts are for development and QA only. Do not use in production.**

Generated by `scripts/seed-test-users.sh` against project `seicofzlgwjqkggscvao`.

---

## Quick-reference table

| Role | Email | Password | Login screen | Grade |
|---|---|---|---|---|
| admin | `admin@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | — |
| counselor | `counselor@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | — |
| student | `student.g9@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | 9 |
| student | `student.g10@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | 10 |
| student | `student.g11@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | 11 |
| student | `student.g12@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | 12 |
| parent | `parent@dekudrenchprep.test` | `LadderTest123!` | School login — Deku Drench Prep | — |
| student (B2C) | `private.user@ladder.test` | `LadderTest123!` | B2C login (Landing → Log in) | 11 |
| founder | `founder.test` (Founder ID) | `LadderTest123!` | Founder backdoor (30-sec logo hold) | — |

---

## Tenants provisioned by the seed script

| Tenant | Type | Slug | ID |
|---|---|---|---|
| Deku Drench Prep | school (B2B) | `deku-drench-prep` | `10000000-0000-0000-0000-000000000001` |

The B2C private-pay user (`private.user@ladder.test`) has `tenant_id = NULL`. No tenant row is created for them — they are a direct B2C subscriber.

---

## Per-role details

### admin@dekudrenchprep.test

**Email:** `admin@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep" (school tenant, slug `deku-drench-prep`)
- `user_profiles`: role = admin, tenant_id = Deku Drench Prep

**What to verify after login:**
- Admin Dashboard loads (banner, KPIs, quick actions)
- Teacher data / Classes / Scheduling sections are accessible
- Admin can manage `counselor_assignments` within Deku Drench Prep
- Admin can manage `parent_links` (approve / remove)
- Admin can read `counselor_notes` for compliance
- School theming controls visible if Founder has enabled the feature flag
- Founder-only actions (cross-tenant analytics, system config) are NOT available

---

### counselor@dekudrenchprep.test

**Email:** `counselor@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `counselor_assignments`: assigned to all 4 school students (grades 9–12)

**What to verify after login:**
- Counselor Dashboard loads (counselor surface, queue card)
- Assigned student caseload shows all 4 students (g9, g10, g11, g12)
- Each student card shows correct grade level
- AI advisor chat history for each student is readable (counselor visibility per ADR-008 §2.11, consent-gated)
- Student essays for each student are readable
- Counselor can create notes; notes with `parent_visible = true` surface to `parent@dekudrenchprep.test`
- Admin-only actions (class management, teacher reviews) are NOT available

---

### student.g9@dekudrenchprep.test

**Email:** `student.g9@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `students` row: grade_level = 9, row ID `20000000-0000-0000-0000-000000000009`
- `parent_links`: linked to `parent@dekudrenchprep.test` (status = active)
- `counselor_assignments`: assigned to `counselor@dekudrenchprep.test`

**What to verify after login:**
- Student Dashboard loads (green "Student" surface)
- Header / profile reads "9th Grade"
- Roadmap shows freshman-year milestones (PSAT intro, interest exploration, clubs)
- Career Quiz CTA is visible (grade 9 student has not completed quiz)
- School-surface tabs appropriate to Deku Drench Prep are shown
- Marketplace entry point follows school tenant config

---

### student.g10@dekudrenchprep.test

**Email:** `student.g10@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `students` row: grade_level = 10, row ID `20000000-0000-0000-0000-000000000010`
- `parent_links`: linked to `parent@dekudrenchprep.test` (status = active)
- `counselor_assignments`: assigned to `counselor@dekudrenchprep.test`

**What to verify after login:**
- Student Dashboard loads
- Header / profile reads "10th Grade"
- Roadmap shows sophomore milestones (PSAT 10, college list intro, leadership activities)
- Career Quiz CTA visible if quiz not yet completed

---

### student.g11@dekudrenchprep.test

**Email:** `student.g11@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `students` row: grade_level = 11, row ID `20000000-0000-0000-0000-000000000011`
- `parent_links`: linked to `parent@dekudrenchprep.test` (status = active)
- `counselor_assignments`: assigned to `counselor@dekudrenchprep.test`

**What to verify after login:**
- Student Dashboard loads
- Header / profile reads "11th Grade"
- Roadmap shows junior milestones (SAT/ACT prep, college research, recommendation requests)
- Career Quiz CTA visible; AI college match suggestions begin appearing
- Application tracker entry point is accessible

---

### student.g12@dekudrenchprep.test

**Email:** `student.g12@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `students` row: grade_level = 12, row ID `20000000-0000-0000-0000-000000000012`
- `parent_links`: linked to `parent@dekudrenchprep.test` (status = active)
- `counselor_assignments`: assigned to `counselor@dekudrenchprep.test`

**What to verify after login:**
- Student Dashboard loads
- Header / profile reads "12th Grade"
- Roadmap shows senior milestones (application deadlines, financial aid, decision day)
- Application deadline alerts are surfaced prominently
- College list shows acceptance-probability tiers (reach / match / safety)

---

### parent@dekudrenchprep.test

**Email:** `parent@dekudrenchprep.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Sign in through your school" → select "Deku Drench Prep" → sign in

**DB setup:**
- Tenant: "Deku Drench Prep"
- `parent_links`: linked to all 4 school students (g9, g10, g11, g12) with status = active

**What to verify after login:**
- Parent Dashboard loads
- Multi-child digest shows 4 child cards (one per grade)
- Each card shows the correct grade level and activity summary
- Parent can see counselor notes where `parent_visible = true`
- Parent cannot edit student data (read-only RLS enforced)
- Switching between child cards updates the displayed roadmap grade

---

### private.user@ladder.test  (B2C private-pay)

**Email:** `private.user@ladder.test`
**Password:** `LadderTest123!`
**Login screen:** Landing screen → "Log in with your ID" (B2C login)

**DB setup:**
- `tenant_id` = NULL — no school affiliation, direct B2C subscriber
- `students` row: grade_level = 11, row ID `20000000-0000-0000-0000-000000000099`
- No `parent_links` row
- No `counselor_assignments` row

**What to verify after login:**
- Student Dashboard loads (green "Student" surface, Ladder default branding — no school theme)
- Header / profile reads "11th Grade"
- Career Quiz CTA is visible
- Marketplace entry point is visible (B2C user has direct access per ADR-008 §3)
- AI advisor chat is accessible
- School-surface tabs (class catalog, scheduling, counselor queue) are NOT shown
- No counselor or parent sidebar

---

### founder.test

**Founder ID (login field):** `founder.test`
**Password:** `LadderTest123!`
**TOTP secret:** `JBSWY3DPEHPK3PXP`
**Login screen:** Founder backdoor — 30-second press-and-hold on the Ladder logo on the landing screen

**How to reach it:**
1. Launch the app to the landing screen.
2. Press and hold the Ladder logo mark for 30 seconds until the Founder Login screen slides in.
3. Enter Founder ID: `founder.test`, Password: `LadderTest123!`, TOTP: (see below).

**How the login email works internally:** The FounderLoginView converts Founder ID `founder.test` to `founder.test@ladder.internal` before calling Supabase Auth. This is the email stored in `auth.users`. You never type the email directly — only the Founder ID field.

**TOTP setup — production / Release builds:**
1. Open Google Authenticator, Authy, or 1Password.
2. Add a new account manually with secret `JBSWY3DPEHPK3PXP`.
3. Name it "Ladder founder.test" so it does not clash with real secrets.
4. Enter the 6-digit rotating code shown in the app into the TOTP field.

**TOTP in DEBUG builds:**
- The app may fall through to a DEBUG bypass if the `founder-login` Edge Function is not yet deployed or returns a non-200. In that case you may see the Founder Dashboard without a valid TOTP.
- This bypass is intentional during development (commit a78b00d schema gap note). It does not exist in Release builds.

**DB setup:**
- No tenant (founders are cross-tenant; the `founder_has_no_tenant` DB constraint enforces this)
- `founder_users` row: display_name = "Founder Test", `totp_secret_cipher` = `JBSWY3DPEHPK3PXP` (plaintext placeholder — in production this field holds a KMS-wrapped ciphertext)

**What to verify after login:**
- Founder Dashboard loads (dark, utilitarian surface)
- Schools / Solo / System tabs are visible
- School card for "Deku Drench Prep" is listed with aggregate data (student count, token usage, success rate)
- School theming controls are accessible from the Deku Drench Prep card (theme_primary_color, theme_accent_color, enabled_features columns from migration 0009)
- Student PII data, grades, and AI chat content are NOT shown per §14.4
- `role = founder` and `tenant_id = null` in the decoded JWT (verify via jwt.io if needed)

---

## How to regenerate

```sh
# From the repo root — fill in SUPABASE_SERVICE_ROLE_KEY in .env first
bash scripts/seed-test-users.sh
```

The script is idempotent. Re-running it detects existing users by email (via the Auth Admin API list endpoint), skips them, and re-applies all DB row upserts with `Prefer: resolution=ignore-duplicates`. It will not create duplicate rows or duplicate users.

---

## Security reminder

- These credentials are hard-coded in this doc and in the seed script. They are for the dev/QA project `seicofzlgwjqkggscvao` only.
- The seed script guards against running against a different project ref — it will exit with an error if `SUPABASE_URL` does not match.
- `SUPABASE_SERVICE_ROLE_KEY` bypasses RLS. It is read from `.env`, which is gitignored. Never commit `.env`.
- If these credentials ever appear in git history, rotate the service role key immediately from the Supabase dashboard.
