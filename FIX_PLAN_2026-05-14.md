# LADDER v1.0 FIX PLAN — 2026-05-14

Shared ground-truth doc for the 16 parallel fix agents. If you are a sub-agent reading this: find your lane below, fix ONLY your files, do NOT touch anyone else's.

## Decisions (locked, do not re-litigate)
- **D1** Real edge URL: `https://seicofzlgwjqkggscvao.supabase.co` (from `AppConfiguration.swift:34`). Required env, NO fallback.
- **D2** Parent multi-child dashboard: replace with "Coming soon" placeholder (no feature flag).
- **D3** Gemini models: `gemini-2.5-flash` for SIA chat, `gemini-2.5-pro` for counselor brief.
- **D4** Counselor scope: caseload-only (revoke tenant-wide read).
- **D5** Target launch: 2026-05-27; slip to 6/10 acceptable if Gemini red-team fails.

## Source audits
- `SECURITY_AUDIT_2026-05-14.md`
- `BUG_HUNT_2026-05-14.md`
- `INTENT_AUDIT_2026-05-14.md`
- `AI_AGENT_AUDIT_2026-05-14.md`

## File ownership (NO overlap allowed)

| Agent | Owns |
|---|---|
| A1 | `LadderBackend/supabase/migrations/0020_founder_totp_decrypt.sql` (NEW); `LadderBackend/supabase/config.toml` (create if missing) |
| A2 | `LadderBackend/supabase/functions/founder-login/index.ts`; `LadderBackend/supabase/functions/employee-login/index.ts` (NEW) |
| A3 | `LadderBackend/supabase/functions/invite-redeem/index.ts`; `LadderBackend/supabase/migrations/0021_invite_hmac_hash.sql` (NEW) |
| A4 | `LadderBackend/supabase/functions/ai-gateway/index.ts` |
| A5 | `LadderBackend/supabase/migrations/0022_founder_restrictive_caseload.sql` (NEW) |
| B1 | `LadderApp/Services/Auth/SupabaseAuthService.swift` |
| B2 | `LadderApp/Services/AI/AIGatewayClient.swift`, `LadderApp/Services/Flags/FlagClient.swift`, `LadderApp/Services/Audit/AuditClient.swift`, `LadderApp/App/Configuration/AppConfiguration.swift`, `Config/Base.xcconfig`, `Config/Secrets.xcconfig` |
| B3 | `LadderApp/Services/AI/Context/StudentContextBuilder.swift` |
| C1 | `LadderApp/Features/Student/ScheduleBuilder/*` |
| C2 | `LadderApp/Features/Student/CareerQuiz/*` |
| C3 | `LadderApp/Features/Auth/B2CLogin/B2CLoginView.swift`, `LadderApp/Features/Backdoor/EmployeeLoginView.swift`, `LadderApp/Features/Founder/Login/FounderLoginView.swift` |
| C4 | `LadderApp/Features/Parent/ParentDashboardView.swift`, `LadderApp/App/SignedInRouter.swift` |
| D1 | `LadderApp/Services/Legal/LegalTexts.swift` |
| D2 | `LadderApp/Services/Engines/ContentModerationService.swift`, plus locate and edit `OfflineQueueManager.swift` |
| D3 | `LadderApp/Info.plist` (or where set), new scene-phase overlay file, `LadderApp/Services/AI/MemoryExtractorService.swift` |
| D4 | Delete legacy `Ladder.xcodeproj` (verify `project.yml`-generated one is the canonical one) |

## Rules for every sub-agent
1. Branch is already `fix/v1.0-audit-sweep`. **Do not commit. Do not push. Do not switch branches.**
2. Edit ONLY your files. If you need to reference a file outside your scope, READ it but DO NOT EDIT it. If you find a bug in another agent's file, write it to `OUT_OF_SCOPE_FINDINGS.md` and append your name + one line.
3. Verify your changes compile / type-check in isolation if possible. Swift agents: `swift build` won't work without xcodegen → just verify syntax with `swiftc -parse` on the file if you want, else trust the human review.
4. Return a ≤400-word diff summary listing each finding closed and the lines touched.
